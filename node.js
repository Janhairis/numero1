/*Write a program that reads the user input one line at a time. 
Every time the user inputs a new line, the program will show all the 
lines that have been input before.*/

// var history = []

// rl.on('line', function (input) {
// clear()
// // history += input + '\n'
// history.push(input)
// console.log(history.join('\n'))
// console.log('write something: ')
// })

// clear()
// console.log('write something: ')

/*Write a program that the user input one line at a time. 
It would expect the user input to be a sequence of numbers and space.
For example: "3 4 9".  The program will add all the numbers and log the result. 
The program will ignore any non-numerical input, but would still add up the rest.
For example, if the user enters: "3 a 4" the program should log `7`.*/
 

// Your Answer:
// function addNumbers(arr){
// // ['4', '5', '9']
// var sum = 0
// for (var i = 0; i < arr.length; i++){
// var num = Number(arr[i])
// if (!isNaN(num)){
// sum += num
// }
// }
// return sum
// }

// rl.on('line', function (input) {
// clear()
// var arr = input.split(' ')
// var sum = addNumbers(arr)
// console.log(sum)
// console.log('write some numbers: ')
// })

// clear()
// console.log('write some numbers: ')